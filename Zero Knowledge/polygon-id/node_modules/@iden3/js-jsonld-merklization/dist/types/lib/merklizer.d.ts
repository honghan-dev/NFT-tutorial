import { Hasher, Value } from './types/types';
import { NodeObject } from 'jsonld';
import { Merkletree, Hash, Proof } from '@iden3/js-merkletree';
import { RDFEntry } from './rdf-entry';
import { Path } from './path';
import { MtValue } from './mt-value';
export declare class Merklizer {
    readonly srcDoc: string | null;
    readonly mt: Merkletree | null;
    readonly hasher: Hasher;
    readonly entries: Map<string, RDFEntry>;
    compacted: NodeObject | null;
    constructor(srcDoc?: string | null, mt?: Merkletree | null, hasher?: Hasher, entries?: Map<string, RDFEntry>, compacted?: NodeObject | null);
    proof(p: Path): Promise<{
        proof: Proof;
        value?: MtValue;
    }>;
    mkValue(val: unknown): Value;
    resolveDocPath(path: string): Promise<Path>;
    root(): Hash;
    rawValue(path: Path): unknown;
    private rvExtractArrayIdx;
    static merklizeJSONLD(docStr: string): Promise<Merklizer>;
}
